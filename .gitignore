# Downloaded sources — never committed, regenerated by `make ingest`
ingest/raw/

# wasm-pack output — built by CI and by `make wasm`, never committed (spec §5)
web/src/wasm/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Rust
target/

# Node
node_modules/
web/.next/
web/out/
web/public/data/

# OS / editor
.DS_Store
*.tsbuildinfo
